# Documentation only. Leave every value empty.
# Put real values in a protected environment or secret file outside the report
# directory. Never paste them into chat or commit them to a report package.

ATTACKER_EMAIL=
ATTACKER_PASSWORD=
VICTIM_EMAIL=
VICTIM_PASSWORD=
# Add only variable names that the supplied report actually requires.
